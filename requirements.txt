# ════════════════════════════════════════════════════════════
# StocksX — 依賴安裝指引
#
# 依賴統一定義在 pyproject.toml（PEP 621），此文件僅作參考。
# ════════════════════════════════════════════════════════════

# ─── 推薦安裝方式 ───
# pip install -e .              # 核心依賴（可編輯模式）
# pip install -e ".[dev]"       # + 開發工具
# pip install -e ".[ml]"        # + 深度學習（TF/PyTorch）
# pip install -e ".[nlp]"       # + NLP（Transformers）
# pip install -e ".[rl]"        # + 強化學習
# pip install -e ".[all]"       # 全功能安裝
#
# Docker 環境：依賴由 Dockerfile 處理，無需手動安裝。
# ════════════════════════════════════════════════════════════
